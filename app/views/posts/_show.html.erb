
<div class="flexslider">
  <ul class="slides">

<% @post.assets.each do |image| %>
  <li>
	<%#= link_to image_tag(image.asset.url(:large)), image.asset.url(:original) %>
	<%= image_tag(image.asset.url(:large)) %>
	</li>
<% end %>
  </ul>
</div>
